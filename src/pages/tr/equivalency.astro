---
import BaseLayout from '@layouts/BaseLayout.astro';

const base = import.meta.env.BASE_URL === '/' ? '' : import.meta.env.BASE_URL.replace(/\/$/, '');
---

<BaseLayout title="Ders Eşleştirme" description="Ders karşılık bulucu aracı">
  <section class="redirect-page">
    <div class="container">
      <div class="redirect-content">
        <h1>🔄 Yönlendiriliyor...</h1>
        <p>Ders Eşleştirme aracına yönlendiriliyorsunuz.</p>
        <p class="hint">Otomatik yönlendirme çalışmazsa, <a href={`${base}/equivalency`}>buraya tıklayın</a>.</p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .redirect-page {
    padding: var(--space-20) 0;
    text-align: center;
  }
  .redirect-content h1 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-4);
  }
  .redirect-content p {
    color: var(--text-secondary);
    margin-bottom: var(--space-4);
  }
  .redirect-content .hint {
    font-size: var(--text-sm);
    color: var(--text-tertiary);
  }
  .redirect-content a {
    color: var(--primary-600);
    text-decoration: underline;
  }
</style>

<script define:vars={{ base }}>
  // Redirect to English equivalency page
  window.location.href = base + '/equivalency';
</script>
